---
status: draft
version: v0.41
updated: 2026-08-11
audience: 台灣助人工作者
---

# 生命歷程時間軸設計方向

## 核心意圖

使用者打開工具後，第一眼先看見生命歷程時間軸，而不是先被要求選擇輸入方式。新增與編輯是閱讀情境中的次要動作，只有按下「新增事件」、「AI 分析」或「編輯」後，才以彈出視窗顯示對應操作。

## 資訊層級

1. 一眼：時間軸、事件數、人物數、分類覆蓋與篩選。
2. 操作：新增事件、AI 分析、左右瀏覽。
3. 深入：點選事件後查看五欄摘要；需要修改時才進入編輯彈窗。

## 互動規則

- 根網址與 `#tool` 都直接進入時間軸。
- 「新增事件」開啟手動輸入彈窗；「AI 分析」開啟同一彈窗的 AI 模式。
- 彈窗內可在「手動輸入」與「AI 分析」切換，不返回另一個頁面。
- 手動新增或編輯完成後關閉輸入彈窗，定位新事件並開啟事件摘要，提供可見回饋。
- AI 草稿可在彈窗內逐筆確認；最後一筆完成後關閉彈窗並定位事件。
- `Esc`、關閉按鈕與點擊背景可關閉彈窗；關閉後焦點回到原觸發按鈕。
- AI 分析進行時鎖定會中斷工作的關閉與切換控制。

## 視覺與元件

- 延用現有暖白、深綠與分類色，不建立新的品牌語言。
- 時間軸是主視覺；不使用裝飾圖片。既有首屏情境圖退出工具流程，避免與真實資料競爭注意力。
- 桌面輸入彈窗最大寬度約 760px；手機版採接近底部面板的滿寬配置。
- 彈窗標題列與模式切換在內容捲動時維持可見，主要動作保持清楚。
- 所有輸入維持 16px 手機字級、44px 以上觸控高度、明確焦點樣式。

## 內容原則

- 頁面文字只說明下一步，不重複解釋系統。
- 事件仍只有年度、月份、人物、大分類與摘要五欄。
- 時間軸預設不顯示摘要；點擊事件才揭露。
- 空白狀態直接提供「新增第一筆事件」與「使用 AI 分析」兩個動作。

## 驗收

- 首次開啟根網址與 `#tool` 都直接看見時間軸標題及新增按鈕。
- 手動新增、AI 文字分析、事件摘要、編輯與刪除入口都能從時間軸完成。
- 彈窗可用鍵盤開啟、切換、送出與關閉，焦點回復正確。
- 390px 無整頁橫向溢出，彈窗不截斷按鈕，時間軸仍可獨立左右滑動。
- `prefers-reduced-motion` 下不依賴動效傳達狀態。
