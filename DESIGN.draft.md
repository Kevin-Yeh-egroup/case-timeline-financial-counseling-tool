---
status: draft
version: v0.43
updated: 2026-08-18
audience: 台灣助人工作者
---

# 生命歷程時間軸設計方向

## 核心意圖

使用者打開工具後，第一眼先看見生命歷程時間軸，而不是先被要求選擇輸入方式。首頁只保留「新增事件」一個主要動作；開啟後預設手動新增，也可在同一個彈出視窗切換到「AI 協助整理」。

## 資訊層級

1. 一眼：時間軸、事件數、人物數、分類覆蓋與篩選。
2. 操作：新增事件、左右瀏覽。
3. 輸入：手動填寫五欄；需要時才切換 AI，以貼上文字或語音輸入建立待整理內容。
4. 深入：點選事件後查看五欄摘要；需要修改時才進入編輯彈窗。

## 互動規則

- 根網址與 `#tool` 都直接進入時間軸。
- 「新增事件」開啟同一個輸入彈窗，預設顯示「手動新增」，並提供次要分頁「AI 協助整理」。
- AI 區塊將原「快速貼上資料」改名為「貼上待整理內容」，整理動作為「整理成待確認草稿」。
- AI 區塊內提供「貼上文字／語音輸入」切換；語音只把辨識文字放入可編輯文字框，不會自動開始 AI 整理。
- 語音輸入具備開始、停止、辨識中、不支援、未取得麥克風權限、無語音與連線失敗狀態；切換模式或關閉彈窗時停止收音。
- AI 只產生可編輯的五欄草稿；每筆都要由使用者確認後才加入時間軸。
- 「編輯這筆事件」沿用同一個彈窗並帶入既有資料。
- 編輯既有事件時不顯示輸入方式切換，避免把編輯誤解為重新分析。
- 手動新增或編輯完成後關閉輸入彈窗，定位新事件並開啟事件摘要，提供可見回饋。
- `Esc`、關閉按鈕與點擊背景可關閉彈窗；關閉後焦點回到原觸發按鈕。

## 視覺與元件

- 延用現有暖白、深綠與分類色，不建立新的品牌語言。
- 時間軸是主視覺；不使用裝飾圖片。既有首屏情境圖退出工具流程，避免與真實資料競爭注意力。
- 桌面輸入彈窗最大寬度約 760px；手機版採接近底部面板的滿寬配置。
- 彈窗標題列在內容捲動時維持可見，主要動作保持清楚。
- 所有輸入維持 16px 手機字級、44px 以上觸控高度、明確焦點樣式。

## 內容原則

- 頁面文字只說明下一步，不重複解釋系統。
- 首屏不顯示宣傳標語、版本資訊或工具規格說明；欄位本身可理解時，不再額外加提示。
- 事件仍只有年度、月份、人物、大分類與摘要五欄。
- AI 協助不增加診斷、推測或處遇欄位；缺少月份時維持待確認，不自動猜測。
- AI 區塊提醒使用去識別內容，並明示輸出需要人工確認。
- 語音辨識由瀏覽器服務提供；介面提醒使用者不要說出身分證、電話、地址等可識別資料。
- 時間軸預設不顯示摘要；點擊事件才揭露。
- 空白狀態只提供「新增第一筆事件」，不分散下一步選擇。

## 驗收

- 首次開啟根網址與 `#tool` 都直接看見時間軸標題及新增按鈕。
- 手動新增、AI 協助整理、事件摘要、編輯與刪除入口都能從時間軸完成。
- AI 草稿可逐筆修改、確認或不採用；未確認草稿不會出現在時間軸。
- 語音辨識結果可在送往 AI 整理前修改，且停止或關閉後不再持續收音。
- API 無法使用時可退回本機基本規則，且介面不把本機結果誤稱為外部 AI 完成。
- 彈窗可用鍵盤開啟、送出與關閉，焦點回復正確。
- 390px 無整頁橫向溢出，彈窗不截斷按鈕，時間軸仍可獨立左右滑動。
- `prefers-reduced-motion` 下不依賴動效傳達狀態。
