# v0.43 預覽驗收清單

## 範圍

- [x] 每筆事件可見欄位只有：年度、月份、事件人物、事件大分類、事件摘要。
- [x] 決策／處遇、影響、待釐清、信心、敏感度、起訖年、年齡、事件清單、檔案匯入與匯出已退出本版介面。
- [x] 新增表單、編輯表單與摘要 dialog 使用同一組五欄。
- [x] 月份未知時保留空白／月份待確認，不自動猜測。

## 輸入

- [x] 首頁直接顯示時間軸，不再顯示入口頁或獨立的「開始整理」區塊。
- [x] 首頁操作區只保留「新增事件」；空白狀態也只提供新增事件。
- [x] 首屏已移除宣傳標語、版本卡、工具規格說明與重複操作提示。
- [x] 「新增事件」與空白狀態按鈕開啟五欄輸入彈窗。
- [x] 新增彈窗預設「手動新增」，並可切換「AI 協助整理」。
- [x] 原「快速貼上資料」改名為「貼上待整理內容」，動作名稱為「整理成待確認草稿」。
- [x] AI／本機規則只產生可編輯草稿，逐筆確認後才加入時間軸。
- [x] AI 區塊可切換「貼上文字／語音輸入」，語音辨識結果先進入可編輯文字框，不會自動整理或加入時間軸。
- [x] 語音輸入具備開始、停止、辨識中、不支援、權限拒絕、無語音與連線失敗狀態。
- [x] 「編輯這筆事件」沿用同一個彈窗並帶入既有五欄資料。
- [x] 編輯既有事件時隱藏輸入方式切換。
- [x] 彈窗可用關閉按鈕、背景與 Esc 關閉，關閉後回復原觸發點焦點。
- [x] 本次加入瀏覽器語音輸入，不加入檔案匯入。

## 閱讀與互動

- [x] 閱讀首頁只顯示概況、篩選與時間軸，不常駐事件全文清單。
- [x] 事件分類以文字列標示，不只靠顏色區分。
- [x] 事件按鈕顯示月份與人物；點擊、Enter 或 Space 才開啟五欄摘要。
- [x] 摘要 dialog 關閉後將焦點還給原事件。
- [x] 事件按鈕的 accessible name 包含年月、分類、人物與查看摘要。
- [x] 時間軸上方提供同步橫向捲動軌、較早／較晚按鈕及方向鍵操作。
- [x] `prefers-reduced-motion` 下移除非必要平滑捲動與動畫。

## 相容與安全

- [x] 保留舊 `caseTimelineToolState`，只讀取並遷移到新 key，不清除或覆寫舊資料。
- [x] 舊事件摘要以 `summary`、`fact`、`title` 順序相容。
- [x] 舊資料缺月份時不補 1 月。
- [x] HTML、robots.txt 與 Vercel header 維持 review-stage noindex 設定。
- [x] API 回應使用 `Cache-Control: no-store`。
- [x] AI 區塊提醒先移除可識別資訊，README 說明文字可能送往已設定的 AI 供應商。
- [x] 語音區塊提醒瀏覽器會提供語音辨識服務，且不得說出可識別資訊。
- [x] README 明確標示 standalone 預覽尚未實作帳號驗證，正式使用需整合登入與權限邊界。

## 本機驗證

- [x] JavaScript 語法與差異檢查通過。
- [x] HTML 59 個 ID 均唯一、54 個 JavaScript ID 綁定皆有對應元素；兩個 dialog 結構與 JavaScript 語法檢查通過。
- [x] 本機 HTTP 200；回傳 v0.43、時間軸首頁與手動／AI 協助新增 dialog。
- [x] 桌機真人路徑：時間軸 → 手動新增 → 摘要 → 編輯，以及 AI 協助 → 語音轉寫／文字貼上 → 草稿修改 → 確認加入／不採用。
- [x] AI API 無金鑰回應為 `local-fallback`、`Cache-Control: no-store`；前端 API 不可用時也成功產生本機規則草稿並明示來源。
- [x] 390×844：AI 彈窗為滿寬底部面板，`innerWidth=390`、`documentScrollWidth=390`，彈窗左右邊界皆在 viewport 內。
- [x] 頂部橫向控制不用先垂直捲到底即可操作長時間軸。
- [x] 應用程式未出現 script／執行錯誤；瀏覽器擴充功能本身的 message-channel 訊息不列入應用程式錯誤。
- [x] 去識別測試人物在手動新增、摘要、編輯及 AI 草稿確認中保持原始內容。
- [x] 語音模擬路徑：開始 → 即時轉寫 → 停止 → 修改文字 → 整理草稿；另驗證不支援與麥克風權限拒絕狀態。
- [x] 390×844 語音控制、文字框與整理按鈕無水平溢出或截斷；`documentScrollWidth=390`，彈窗邊界位於 viewport 內。

## 發布狀態

- 本機版本：`v0.43-ai-voice-assisted-timeline`
- 本次只產生本機預覽，尚未 commit、push、更新 Draft PR 或部署。
- 更新 Draft PR 或正式發布都需 Kevin 預覽後明確核准。
